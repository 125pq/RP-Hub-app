# RP-Hub Android 更新日志

按热修复版本记录 Android 侧改动。每次发布前，在本文件顶部追加当前版本条目；发布流水线会自动把对应版本段落注入 Release notes。

## 1.8.8

- 本地控制中心：新增手动检查更新；更新下载支持在 GitHub 与 Gitee 源之间切换，并在下载源异常时自动尝试其他源。
- 万相广场：支持原站/镜像源切换；角色卡下载改用系统文件选择器保存，保留登录态并兼容内嵌页面下载。
- Android 界面适配：升级 Capacitor 8 系统栏处理，保留原生 WebView 深色适配与“跟随系统”开关；完善安全区和键盘避让，修复万相广场内嵌页面布局及侧边栏 Logo 左边距。
- 首次启动：增加 RP-Hub 来源与防倒卖说明，确认后继续执行更新检查。

## 1.8.4.3

- Android 更新下载：缩短慢速源宽限期并提高最低下载速率阈值，避免更新检查在异常慢速源上等待过久。
- 上游同步：恢复 `assets/js/app.js` 的上游混合换行，减少未来与上游合并时的换行噪声冲突。
- 工程校验：新增 push / pull request 纯校验工作流，覆盖语法、平台、上游同步、Web 构建与产物校验。

## 1.8.4.2

- 平板/桌面端：修复侧边栏收起后外壳宽度不切换、正文区域不扩展的问题（侧边栏宽度类改为互斥切换，避免离线预编译 CSS 的级联顺序导致收起失效）。
- 平板/桌面端：恢复消息正文宽度为 `min(56rem, 72%)`，修复侧边栏收起后聊天正文不随可用空间扩展、浪费横向空间的问题。
- 新增 EOL/空白 churn 自动守卫测试，防止未来 patch 引入整文件换行符重写。

## 1.8.3.6

- 万相广场等内嵌页面下载：为 WebView 挂载 `DownloadListener`，点击下载改由系统 `DownloadManager` 接管，并显式保存到公共「下载」目录（`Downloads`），修复内嵌站点点击下载无任何反馈、以及下载成功后文件在相册/文件管理器中找不到的问题。
- 大文件聊天记录导入：改为流式逐分支读取并即时写入本地存储，跳过昂贵的重复统计，修复上百 MB 的 `.jsonl` 聊天记录导入长时间卡死、内存占用过高的问题。
- 聊天渲染性能：缓存每条消息的正文剥离与时间线字数统计，减少打开侧边栏或切换界面时聊天列表的重复计算卡顿。

## 1.8.3.5

- 滚动聊天时暂停对话内复杂 UI（可执行 HTML 卡片 / UI 模板）的 CSS 动画，滚动停止约 180ms 后自动恢复，改善长聊天滚动流畅度。

## 1.8.3.4

- 多分支聊天导出改为流式写入，降低大文件导出时的内存占用。
- SAF 导出重名文件自动改名去重，保持多段扩展名语义。
- 新增 2.71 变量 UI 到 2.8 正则 UI 的补全与校验脚本。
